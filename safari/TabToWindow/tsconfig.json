{
  "extends": "../../tsconfig.json",
  "include": ["./ts/**/*.ts"],
  "compilerOptions": {
    // "outDir": "./TabToWindow Extension/Resources/js" /* Redirect output structure to the directory. */
  }
}
